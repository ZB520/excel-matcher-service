# 环境变量配置指南

本文档说明如何为 Zeabur 和阿里云函数计算（FC）配置必要的环境变量。

---

## 一、Zeabur 环境变量配置

登录 [Zeabur 控制台](https://zeabur.com)，进入你的项目，在"环境变量"（Environment Variables）中添加以下配置：

### 必需的环境变量

| 环境变量名 | 说明 | 示例值 |
|---|---|---|
| `OSS_ACCESS_KEY_ID` | 阿里云 AccessKey ID | `LTAI5t...`（你的实际 AK） |
| `OSS_ACCESS_KEY_SECRET` | 阿里云 AccessKey Secret | `your_secret_here`（你的实际 SK） |
| `OSS_ENDPOINT` | 阿里云 OSS 访问域名 | `https://oss-cn-hangzhou.aliyuncs.com` |
| `OSS_BUCKET` | 阿里云 OSS Bucket 名称 | `book-company-excel-uploads-v2` |
| `DINGTALK_WEBHOOK` | 钉钉机器人 Webhook 地址 | `https://oapi.dingtalk.com/robot/send?access_token=5b8ef7f0a7d203e858c5b00fdc99572081cf4813d1200aeed339bb217e9c7ad5` |
| `DINGTALK_SECRET` | 钉钉机器人加签密钥 | `SEC830ef80585c1e7cced0b4ef93068d55773fc271190859e98e2aec45c286e0cdc` |

### 配置步骤

1. 进入你的 Zeabur 项目页面
2. 点击你的服务（service）
3. 选择"环境变量"（Environment Variables）标签
4. 点击"添加环境变量"按钮
5. 逐一输入上述环境变量的键和值
6. 保存后，Zeabur 会自动重新部署你的服务

### 验证

部署完成后，访问以下页面确认功能正常：
- `https://你的域名.zeabur.app/` - 单次匹配页面
- `https://你的域名.zeabur.app/upload_to_oss` - 批量上传页面
- `https://你的域名.zeabur.app/download_results` - 下载结果页面

---

## 二、阿里云函数计算（FC）环境变量配置

登录 [阿里云函数计算控制台](https://fcnext.console.aliyun.com/)，找到你的函数 `oss-excel-matcher`，在函数配置中添加环境变量。

### 必需的环境变量

| 环境变量名 | 说明 | 示例值 |
|---|---|---|
| `DINGTALK_WEBHOOK` | 钉钉机器人 Webhook 地址 | `https://oapi.dingtalk.com/robot/send?access_token=5b8ef7f0a7d203e858c5b00fdc99572081cf4813d1200aeed339bb217e9c7ad5` |
| `DINGTALK_SECRET` | 钉钉机器人加签密钥 | `SEC830ef80585c1e7cced0b4ef93068d55773fc271190859e98e2aec45c286e0cdc` |
| `OSS_ACCESS_KEY_ID`（可选） | 阿里云 AccessKey ID（用于生成签名下载链接） | `LTAI5t...` |
| `OSS_ACCESS_KEY_SECRET`（可选） | 阿里云 AccessKey Secret（用于生成签名下载链接） | `your_secret_here` |

> **注意**：
> - FC 函数通过 RAM 角色访问 OSS 进行文件读写，因此 `OSS_ACCESS_KEY_ID` 和 `OSS_ACCESS_KEY_SECRET` 不是必需的
> - 但如果需要在钉钉通知中包含临时下载链接，建议配置这两个变量
> - 如果不配置，钉钉通知中将只包含网页下载地址，不会包含直接下载链接

### 配置步骤

1. 登录阿里云函数计算控制台
2. 进入"函数列表"，找到 `oss-excel-matcher` 函数
3. 点击函数名称，进入函数详情页
4. 点击"配置"标签
5. 在"环境变量"区域，点击"编辑"
6. 添加上述环境变量：
   - 键：`DINGTALK_WEBHOOK`，值：`https://oapi.dingtalk.com/robot/send?access_token=5b8ef7f0a7d203e858c5b00fdc99572081cf4813d1200aeed339bb217e9c7ad5`
   - 键：`DINGTALK_SECRET`，值：`SEC830ef80585c1e7cced0b4ef93068d55773fc271190859e98e2aec45c286e0cdc`
   - （可选）键：`OSS_ACCESS_KEY_ID`，值：你的阿里云 AK
   - （可选）键：`OSS_ACCESS_KEY_SECRET`，值：你的阿里云 SK
7. 点击"保存"

### 验证

上传测试文件到 OSS `tasks/张/` 目录（例如 `玉环新表.xlsx` 和 `玉环旧表.xlsx`），等待 10-20 分钟后：
- 检查钉钉群是否收到通知消息
- 消息中应该 @ 你的手机号（`17737759937`）
- 如果配置了 OSS AK/SK，消息中应包含直接下载链接

---

## 三、用户手机号配置

目前手机号映射硬编码在 `fc_handler.py` 文件中。如需修改，请编辑该文件中的 `_send_dingtalk_notification` 函数：

```python
# 手机号映射（测试阶段）
phone_map = {
    "张": "17737759937",
    "徐": "17737759937",  # TODO: 替换为实际手机号
    "章": "17737759937",  # TODO: 替换为实际手机号
    "李": "17737759937",  # TODO: 替换为实际手机号
}
```

修改后需要重新部署 FC 函数。

---

## 四、网页域名配置

钉钉通知消息中包含网页下载地址，目前使用的是占位符 `https://你的域名.zeabur.app`。

请在以下位置替换为实际域名：

### 1. `fc_handler.py` 中的钉钉通知消息

找到 `_send_dingtalk_notification` 函数，将：
```python
text = f"@{phone} 您好，您的数据处理任务已完成！...\n\n或访问网页查看所有结果：\nhttps://你的域名.zeabur.app/download_results?person={person}"
```

替换为：
```python
text = f"@{phone} 您好，您的数据处理任务已完成！...\n\n或访问网页查看所有结果：\nhttps://你的实际域名.zeabur.app/download_results?person={person}"
```

---

## 五、安全提示

⚠️ **重要安全提示**：

1. **不要将 AccessKey 和 Secret 提交到 Git 仓库**。始终使用环境变量配置，不要写在代码里。
2. **定期轮换密钥**：建议每 3-6 个月更换一次 AccessKey 和钉钉机器人加签密钥。
3. **最小权限原则**：确保阿里云 RAM 角色和 AccessKey 只拥有必要的权限（OSS 读写、FC 执行）。
4. **监控使用情况**：定期检查阿里云和钉钉的使用日志，发现异常及时处理。

---

## 六、常见问题

### Q1: 如何获取阿里云 AccessKey ID 和 Secret？

1. 登录[阿里云控制台](https://homenew.console.aliyun.com/)
2. 点击右上角头像 → "AccessKey 管理"
3. 推荐使用"子用户 AccessKey"（RAM 用户），而非主账号 AccessKey
4. 创建新的 AccessKey 对，记录 AccessKey ID 和 AccessKey Secret

### Q2: 如何获取钉钉机器人 Webhook 和加签密钥？

1. 在钉钉群中，点击右上角"..."→"智能群助手"→"添加机器人"
2. 选择"自定义"机器人（通过 Webhook 接入）
3. 配置机器人名称和安全设置（**必须开启"加签"**）
4. 记录生成的 Webhook 地址和加签密钥（Secret）

### Q3: 为什么上传文件后没有收到钉钉通知？

可能原因：
1. 环境变量配置错误或未配置
2. 钉钉机器人加签计算失败（检查 Secret 是否正确）
3. 函数计算执行失败（查看 FC 函数日志）
4. 手机号映射不正确（目前硬编码在 `fc_handler.py` 的 `_send_dingtalk_notification` 函数中）

### Q4: 为什么下载页面显示"暂无处理结果"？

可能原因：
1. 文件还在处理中，等待 10-20 分钟后刷新页面
2. 上传的文件命名不符合规则（文件名必须包含"新表"或"旧表"）
3. OSS 配置错误，导致 Zeabur 服务无法连接到 OSS
4. 选择了错误的用户名（确保选择上传时使用的姓名）

### Q5: 下载链接显示"链接已过期"怎么办？

临时签名 URL 有效期为 1 小时（钉钉通知中的链接为 24 小时）。如果过期：
1. 刷新下载页面，系统会自动生成新的签名 URL
2. 或者重新访问 `/download_results` 页面重新选择用户

---

## 七、下一步

配置完成后：

1. 将更新后的代码推送到 GitHub
2. Zeabur 会自动重新部署
3. 如果修改了 `fc_handler.py`，需要重新打包上传到阿里云 FC（参考 README 的部署说明）
4. 测试完整流程：
   - 访问 `/upload_to_oss` 上传文件
   - 等待处理完成（10-20 分钟）
   - 检查是否收到钉钉通知
   - 访问 `/download_results` 下载结果

祝使用愉快！
